fix: simplify context handling to prevent duplication

- Modify get_context_messages to only return latest summary
- Remove message history from context to prevent duplication
- Keep message storage for summary generation
- Maintain all other functionality

This change prevents context glitches and message duplication in API calls.
