feat: Add DM support to router

- Added check for DM channels in router's should_handle_message method
- Router will now respond to all direct messages from users
- Maintains existing functionality for server messages
