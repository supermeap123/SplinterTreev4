fix: add proper groq provider prefix handling in API

- Added support for 'groq' provider in OpenPipe API calls
- Uses 'openpipe:groq/' prefix for Groq models
- Maintains 'openpipe:openrouter/' prefix for OpenRouter models
- Updated both streaming and non-streaming API calls
