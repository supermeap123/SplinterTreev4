fix: improve message handling and rate limiting

- Add proper rate limiting with minimum request interval
- Add rate limit lock to prevent concurrent API requests
- Improve message deduplication in ContextCog
- Add request timing tracking in API
- Ensure chronological message ordering
- Fix duplicate message handling in base_cog

This fix prevents duplicate messages, maintains proper conversation order, and adds robust rate limiting to prevent API errors.
