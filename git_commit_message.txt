refactor(api): Replace conditional logic with explicit provider prefixes

- Replace if/elif statements with explicit provider prefix mapping
- Add comprehensive list of supported providers (anthropic, mistral, google, etc.)
- Improve maintainability by centralizing provider prefix definitions
- Ensure consistent model name prefixing across all API calls
