fix(bot): ensure Llama32_3B cog loads before context_cog

- Modify cog loading order to load Llama32_3B first
- This ensures the summarization model is available when context_cog initializes
- Should fix "No summaries found" error by enabling summary generation

The context_cog depends on Llama32_3B for generating summaries, so loading
it first ensures proper initialization of the summarization functionality.
