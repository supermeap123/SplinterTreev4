Add HTTP Referrer and X-Title headers

- Updated shared/api.py to include custom headers:
  * Added 'Referer' header pointing to GitHub repo
  * Added 'X-Title' header with value 'GwynTel'
- Modified both streaming and non-streaming OpenPipe API request methods
- Added headers to aiohttp ClientSession and OpenAI client initialization
