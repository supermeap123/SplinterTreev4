enhance: Improve conversation context handling

- Add per-channel locks to prevent concurrent summary generation
- Implement backoff retry logic for summary generation
- Improve summary system prompt for better context preservation
- Increase message fetch limit to match utils.py (up to MAX_CONTEXT_WINDOW)
- Add rate limiting to summary checks (once per hour per channel)
- Include conversation metadata in context
- Optimize summary integration by including only most recent summary
- Add better error handling and logging throughout

These changes improve the reliability and efficiency of context handling while ensuring better conversation continuity.
