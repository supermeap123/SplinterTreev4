fix: Update context_cog to use configured message limits

- Modified get_context_messages to use channel-specific or default context window
- Added check to ensure limit doesn't exceed MAX_CONTEXT_WINDOW
- Updated SQL query to use dynamic limit from config instead of hardcoded 10 messages
