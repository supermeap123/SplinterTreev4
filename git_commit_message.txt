fix: Improve router cog model selection and error handling

- Remove quotes from model names in selection prompt
- Add quote stripping from model selection response
- Add more detailed logging for debugging
- Update error messages to be more informative
- Fix fallback logic to properly handle Ministral cog

The router now correctly handles model selection and provides better error reporting when models aren't found.
